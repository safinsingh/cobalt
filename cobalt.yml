round: Mock 0.1
inject_dir: injects/

boxes:
  - name: tempest
    ip: 1

    services:
      - alias: Wordpress
        type: http_content
        path: /
        content: meow

      - alias: OpenSSH
        type: ssh

injects:
  - title: Create SSH server
    source: ssh.pdf
    type: service
    services:
      - alias: CoolSSH
        type: ssh

  - title: Respond to IT
    source: it.pdf
    type: response

subnets:
  - 10.10.11.x
  - 10.10.12.x
  - 10.10.13.x
  - 10.10.14.x
  - 10.10.15.x
